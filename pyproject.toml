[build-system]
requires = ["setuptools>=77", "wheel", "torch"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
]
ignore = [
    "E501",
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",
]

[tool.mypy]
python_version = "3.12"
files = [
    "run.py",
    "scripts",
    "vipe",
]
exclude = [
    "^build/",
    "^third_party/",
    "^vipe\\.egg-info/",
    "^vipe_results/",
    "^vipe/ext/lietorch/",
    "^vipe/priors/",
]
check_untyped_defs = false
follow_imports = "silent"
warn_redundant_casts = true
warn_unused_configs = true
warn_unused_ignores = false

[[tool.mypy.overrides]]
module = [
    "ffmpeg",
    "Imath",
    "OpenEXR",
    "pandas",
    "pandas.*",
    "scipy",
    "scipy.*",
    "tqdm",
    "tqdm.*",
    "vipe_ext",
    "vslam",
    "yt_dlp",
]
ignore_missing_imports = true

[project]
name = "nvidia-vipe"
version = "1.2.0"
description = "NVIDIA Video Pose Engine"
authors = [
    {name = "The ViPE Authors", email = "jiahuih@nvidia.com"},
]
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.12,<3.13"
dependencies = [
    "torch",
    "torchvision",
    # Locally-built PyTorch wheels do not declare this runtime dependency.  It
    # supplies libcudnn.so.9, which the CUDA build in third_party links against.
    "nvidia-cudnn-cu12==9.10.2.21; sys_platform == 'linux'",
    "click",
    "einops",
    "gdown",
    "huggingface_hub",
    "hydra-core",
    "imageio[ffmpeg]",
    "kornia",
    "matplotlib",
    "ninja",
    "numpy",
    "omegaconf",
    "opencv-python",
    "OpenEXR<3.3.0",
    "pydantic>=2,<3",
    "pillow",
    "python-pycg",
    "ray",
    "rerun-sdk",
    "safetensors",
    "scipy",
    "timm",
    "tqdm",
    "transformers>=4,<5",
    "viser",
]
keywords = ["computer-vision", "depth-estimation", "nvidia", "pose-estimation", "video"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Image Processing",
]

[project.urls]
Homepage = "https://research.nvidia.com/labs/toronto-ai/vipe"
Repository = "https://github.com/nv-tlabs/vipe"
Paper = "https://research.nvidia.com/labs/toronto-ai/vipe/assets/paper.pdf"

[dependency-groups]
dev = [
    "build>=1,<2",
    "mypy>=1.15,<2",
    "pre-commit>=4,<5",
    "pytest>=8,<9",
    "ruff>=0.12,<1",
    "twine>=6,<7",
]
docs = [
    "hydra-core",
    "mkdocs>=1.6,<2",
    "omegaconf",
    "pydantic>=2,<3",
]

[project.scripts]
vipe = "vipe.cli.main:main"

[tool.uv]
default-groups = []

[tool.uv.extra-build-dependencies]
nvidia-vipe = [
    "torch",
    "nvidia-cudnn-cu12==9.10.2.21",
]

[tool.uv.sources]
torch = [
    {path = "./third_party/torch-2.13.0a0+gitcf30153-cp312-cp312-linux_x86_64.whl"},
]
torchvision = [
    {path = "./third_party/torchvision-0.28.0+8fb8771-cp312-cp312-linux_x86_64.whl"},
]
openexr = [
    {git = "https://github.com/AcademySoftwareFoundation/openexr.git", tag = "v3.2.10"},
]
